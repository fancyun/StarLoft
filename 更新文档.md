## v1.0.0
- 平台首个版本发布。

## v1.0.1
- 修复认证接口返回信息类型错误的问题。

## v1.0.2
- 下游调用调整为平台中转模式：认证结果先回流至平台，再由平台通知或跳转下游；/kyc 页面展示认证结果并倒计时返回，新增「我已完成验证，点击此处」手动查询上游结果入口。

## v1.0.3
- /kyc 页面统一采用 biz_no 参数；认证中状态即展示「我已完成验证，点击此处」手动查询按钮，并保留继续认证入口。
- 修复账户实名计费逻辑：订单金额按用户实际 KYC 单价记录，实名界面实时展示用户 KYC 单价（此前固定为 1 元）。

## v1.0.4
- 后台认证订单列表补充展示用户手机号（订单查询联表获取用户信息）；订单失败状态展示失败原因（result_message 或 result_code）。

## v1.0.5
- 修复认证结果同步逻辑：上游返回「未开始（NOT_STARTED）/进行中（PROCESSING）」时保持订单「认证中」状态，避免误判为失败而提前结束流程。

## v1.0.6
- 前端页面增加加载状态：账户实名、首页、API 管理、余额、个人资料等页面在数据加载完成前展示加载动画，避免默认内容闪现。

## v1.0.7
- 修复姓名校验：正则改用 Go 支持的 Unicode 字符类 \p{Han}（原 \uXXXX 写法在 Go 中始终报错，导致所有姓名均被判定为非法）；校验长度改为按字符数而非字节数计算，并允许首尾存在空格。

## v1.0.8
- 新增管理员修改密码功能：后台右上角登录账号下拉菜单新增「修改密码」入口，需验证旧密码并二次确认新密码。
- 统一时间显示格式：所有页面时间去除末尾时区信息（+08:00），统一展示为 YYYY-MM-DD HH:mm:ss。

## v1.0.9
- 修复发起认证的异常兜底逻辑：扣费失败时回滚新建的订单与实名记录，避免产生「待认证」但无上游 token 的悬挂订单；写入上游 token 失败时退还预扣余额，并将订单标记为超时退款。
- 修复 /kyc 结果页跳转逻辑：认证完成或失败后优先返回下游 return_url；return_url 缺失时退回来源页面，不再错误跳转至平台登录页。

## v1.0.10
- 修复用户账号实名回跳地址：向上游传递的 return_url 由已废弃的 /user/kyc/result 统一改为 /kyc/result；前端 /auth-result 结果页路由与后端同名占位路由、回调重定向一并更名为 /kyc/result。

## v1.0.11
- 新增文档中心 /docs：包含文档总目录（/docs）、API 文档目录（/docs/api）、API v1 详细调用文档（/docs/api/v1）、插件使用教程目录（/docs/plugin）及智简魔方（ZJMF）插件教程（/docs/plugin/zjmf）。
- 移除用户中心「API 管理」页面的接口调用提示信息，改为跳转文档中心查看。
- 修复后台用户列表实名信息显示异常（undefinedundefined）：原因为 GetUserList 未提取 sql.NullString 而直接序列化，现改为提取 kyc_name/kyc_id_card 字符串值。

## v1.0.12
- 平台实名认证增加年龄限制：要求年满 16 周岁，发起认证（Web 账户实名与 API 调用）时根据身份证号中的出生日期计算周岁年龄，未满 16 周岁返回「实名认证需年满 16 周岁」并拒绝发起认证。
- 首页「个人信息」栏在邮箱与注册时间之间增加「实名单价」展示。

## v1.0.13
- 补充 API 文档（/docs/api/v1）回调说明：新增异步通知（notify_url）与同步跳转（return_url）章节，说明通知请求体字段（biz_no/platform_biz_no/status/result_code/result_message/cost）、result_code 结果码含义（1000/2000/3000/4000/6000/6100，其中 6000/6100 不计费自动退款），以及 return_url 以 GET 302 跳转且不附加参数、需下游主动调用查询接口落地结果。

## v1.0.14
- 修复账户实名 return_url 页面 404：将 Web 端发起认证的 usePlatformURLs 由 false 改为 true，使上游认证完成后先经由平台 /kyc 中转页（KYCVerify）按 biz_no 查询结果，再返回用户实名页；同时将 KYC.vue 的 return_url 由 /kyc/result 改为 /user/kyc，认证完成后正确返回「账户实名」页面。
- 清理遗留死代码：删除已废弃的前端 /kyc/result 结果页路由及其 AuthResult.vue、后端同名占位路由 GET /kyc/result，以及未注册路由的 FinAuthReturnCallback 同步回调处理函数。
- 修正账户实名后端默认 return_url：将 StartAuthForWeb 的空缺回跳地址默认值由占位地址 https://yourdomain.com/kyc/result 改为 /user/kyc，与前端实际使用地址保持一致。

## v1.0.15
- 修复插件单次点击导致平台重复发起验证的问题：
  - 后端：StartAuth 增加幂等去重——同一 biz_no 已存在进行中订单（status 0/1）时直接复用并返回已有 platform_biz_no/auth_url，避免重复创建任务与重复扣费；GetOrderByBizNo 增加 ORDER BY id DESC LIMIT 1，确保获取最新订单。
  - 插件：ZjmfMfcwPlugin.php 将每次随机生成的 biz_no（日期+随机数）改为「同一用户+同一身份证」的固定值（ZJMF_uid_身份证MD5前8位），配合后端去重避免重复创建任务。

## v1.0.16
- 修复 admin token 过期后被路由守卫/401 拦截器错误跳转至 /login 而非 /admin/login，导致无法登录的问题：
  - 前端 request.ts 响应拦截器：401 时按请求路径区分管理员接口（url 以 /admin/ 开头）与普通用户接口，管理员接口清除 admin_token 并跳转 /admin/login，普通接口跳转 /login。

## v1.1.0
- 业务规则调整：账户实名永久绑定且免费，API 需实名后开通：
  - 账户实名认证不可修改：实名成功后信息永久绑定，移除「更换实名」相关能力（后端 /user/kyc/replace 路由、服务与仓储层 Replace/ClearUserKYCInfo 方法、前端更换实名按钮与入口全部删除）。
  - 账户实名由收费改为免费：Web 端账户实名（/user/kyc）不再扣取余额，仅 API 下游业务调用（/api/v1/kyc/start）仍按 KYC 单价从余额扣费；实名页面移除余额校验与费用展示。
  - API 需实名后开通：注册时不再生成 API Key，账户实名成功后自动下发 API Key/Secret；重置 API 密钥同样需先完成实名认证，否则返回 403。

## v1.1.1
- 恢复缺失的 docker-compose.yml 文件。
- 规范更新文档内容格式与用语。

## v1.1.2
- 修复注册用户报 failed to create user：实名前 api_key 为空字符串，platform_user 表 uk_api_key 唯一索引导致第二个用户插入重复空值冲突；移除该唯一索引（存量库执行 ALTER TABLE platform_user DROP INDEX uk_api_key），空 key 不影响 API 鉴权（中间件已强制校验非空）。

## v1.2.0
- 新增资源包功能：
  - 新增资源包定义表与用户资源包表（resource_pack / user_resource_pack），资源包支持库存限制（-1 不限量，>=0 限量）与上架/下架管理。
  - 管理后台新增「资源包管理」页面，支持资源包创建、编辑、下架；用户中心新增「资源包」页面，展示在售资源包与我的资源包（剩余次数）。
  - 购买资源包直接从余额扣费，不支持直接为资源包付费（需先充值再购买）；购买时在事务内完成库存扣减、余额扣费、余额流水与用户资源包创建，售罄/余额不足时返回对应提示。
  - 认证扣费规则调整为「先扣资源包，再扣余额」：账户实名（超限后）与 API 业务调用计费时优先消耗用户资源包次数，资源包耗尽或无资源包时按平台 KYC 单价从余额扣费；资源包并发耗尽自动回退余额扣费。
- 取消用户个人 KYC 单价设定：统一按平台单价（system_config.kyc_price）扣费，后台移除「修改实名单价」与「余额赠送」功能（赠送本为活动设定，资源包上线后不再需要）。
- 新增免费认证失败次数限制：账户实名（Web）免费认证按账号终身累计失败次数计算（写死为 3 次），累计失败达到 3 次后，再次认证转为计费（先扣资源包，再扣余额），次数不会随时间刷新。
- 人工充值需填报银行流水单号：后台人工充值时必填银行流水单号（bank_serial_no），余额流水记录并展示该单号用于对账。
- API 文档同步更新：说明认证计费优先扣资源包、余额兜底，查询余额返回平台认证单价。

## v1.2.1
- API 密钥生成时机调整：
  - 用户注册（含后台手动创建用户）时自动生成唯一 API Key 并写入数据库，恢复平台用户表 api_key 唯一索引（uk_api_key），从数据库层保证 API Key 唯一；API Secret 不再随注册生成。
  - API Secret 需账户实名认证成功后自动生成下发：实名成功回调（主动查询与异步通知）均会为用户补发 API Secret；历史存量用户（注册时未生成 Key）实名成功后一并补全 API Key。
  - 重置 API 密钥仍要求先完成实名认证，重置时 Key 与 Secret 一并重新生成。
- 用户中心「API 管理」页调整：API Key 注册后即可查看并复制；API Secret 实名通过后展示，新增「显示/隐藏」与「复制」按钮。

## v1.3.0
- 资源包购买流程调整：
  - 用户中心「资源包」页（/user/packs）改为仅展示「我的资源包」：保留余额概览与已购资源包剩余次数列表，无资源包时展示空状态提示。
  - 购买资源包拆分为独立页面（/user/packs/buy）：在售资源包每行展示 3 个，点击「购买」需弹窗确认（展示资源包名称、价格与可获得次数），确认后才发起扣费购买；售罄资源包按钮置灰并标注「已售罄」。
  - 用户中心侧边栏新增「购买资源包」入口，资源包页右上角同步提供「购买资源包」按钮。
- 取消平台 /kyc 认证中转页，认证流程改为全程在用户与上游之间直接完成：
  - return_url 透传：向上游发起认证时，直接将用户/下游传入的 return_url 透传给上游，认证完成后浏览器直接回到该地址，不再先经平台 /kyc 页中转；notify_url 仍为平台回调地址（平台负责结果落地、扣费/退款、下发 API Secret 与通知下游）。
  - 上游认证地址直出：/api/v1/kyc/start 返回的 auth_url 即为上游认证页地址，下游直接用其跳转即可。
  - 删除平台前端 /kyc 页面（KYCVerify.vue）及其公开结果查询接口（GET /api/v1/public/kyc/result 与对应后端处理器/服务方法）。
- 账户实名流程内聚至 /user/kyc：
  - Web 发起认证后直接跳转上游认证页，认证完成后浏览器直接回到 /user/kyc 展示结果。
  - /user/kyc「认证进行中」状态提供「查询认证结果」（同步上游后刷新状态）与「继续认证」（直接跳转上游认证页）操作，不再跳转 /kyc。

## v1.3.1
- 修复下游回调可被伪造的安全漏洞：平台通知下游（notify_url）认证结果时新增 HMAC-SHA256 签名（sign 字段，基于下游的 API Secret 生成），下游校验不通过即拒绝，防止伪造认证结果。
- 修复认证结果查询接口越权问题：按 platform_biz_no 查询认证结果时校验订单归属，非本人订单返回「订单不存在」，防止越权查看他人认证记录。
- 修复日志泄露敏感信息：上游 FinAuth 请求与回调日志对人脸图片、证件号、姓名等敏感字段脱敏，且不再打印携带签名参数的完整请求 URL。
- 清理未使用的回调通知死代码（CallbackService），统一由认证服务签名后通知下游。
- 修复账户实名（Web）订单误向平台自身回调地址发送无意义通知的问题：账户实名流程不再设置下游 notify_url，仅 API 调用显式传入 notify_url 时才通知下游。
- 智简魔方 v10 插件需与本次后端一并升级：插件回调接口已支持 sign 字段签名校验（校验失败返回 401 拒绝）。

## v1.3.2
- 同步平台内嵌文档（/docs）：API v1 文档「异步通知（notify_url）」章节更新为 v1.3.1 后的回调契约，补充 sign 字段、请求体示例、字段说明与 HMAC-SHA256 签名算法，便于下游正确校验回调。

## v1.3.3
- 资源包购买页套餐改为正序展示（按插入顺序排列，从低档位到高档位）。
- 用户菜单优化：
  - 移除「购买资源包」菜单项，购买入口收敛到「我的资源」页内的「购买资源包」按钮。
  - 「资源包」菜单重命名为「我的资源」，页面标题同步更新。
  - 菜单重新排序：首页 → 实名认证 → 余额管理 → 我的资源 → 认证记录 → API 管理 → 账户设置。
- 购买资源包确认弹窗操作按钮明确为「确定」，防止误触。
- init.sql 新增资源包套餐种子数据：0.7/0.6/0.5/0.4 元每次四档共 12 个套餐（100~50万次，库存不限量），已同步写入线上数据库。

## v1.4.0
- 全站前端视觉统一为腾讯云官网设计风格：
  - 主色切换为腾讯云品牌蓝（#006EFF），配套浅蓝渐变与中性灰阶（#1D2129/#4E5969/#86909C），统一小圆角与轻盈阴影，整体观感更专业克制。
  - 全局主题变量集中管理（base.css）：颜色、圆角、阴影、边框统一抽取为 CSS 变量，各页面与组件全部引用变量，便于后续整体换肤。
  - Element Plus 组件主题覆盖（main.css）：按钮、卡片、表格、标签、菜单、弹窗、输入框聚焦态等统一为腾讯云风格，替换默认蓝色 #409EFF。
  - 首页、登录/注册页、用户中心、管理后台、文档中心等页面布局与配色整体对齐：顶部白底下边框、侧边栏白底浅蓝高亮、统计卡片语义色（用户/订单/收入/月度）、图表色随主题变量联动。
  - 清理残留的旧版 Element 配色（旧红 #F56C6C、旧绿 #67C23A、旧灰 #909399），统一替换为主题语义色变量。

## v1.4.1
- 统一用户端与管理端页面格式，消除两端视觉差异：
  - 卡片与标题样式全局化（main.css 新增 .card/.section-title/.card-head）：用户端与管理端统一使用白色圆角卡片 + 带图标与下边框的标题，管理端页面由 el-card 纯文字标题统一改为该结构。
  - 布局对齐：管理端主内容区 padding 与用户端统一为 24px（移除各管理页内部的 20px padding），侧边栏宽度统一为 200px。
  - 筛选行样式（.filter-bar）全局化，管理端用户/订单页与用户端认证记录页统一筛选栏外观。
  - 用户端内部统一：认证记录页标题改为图标式 section-title、个人资料页各卡片标题统一带图标，删除各页重复的 .card/.section-title 样式定义。
  - 补齐 base.css 缺失的间距变量（--gap-sm/--gap-md/--gap-lg/--gap-xl），此前多处引用但未定义导致间距样式失效。

## v1.4.2
- 修复用户端与管理端 Header 及侧边栏的高度、宽度、层级不一致问题：
  - Header 对齐：管理端 Header 补充层级（z-index 100）与用户端一致，移除多余的 box-shadow，两端统一为 60px 白底+下边框；管理端品牌标识「管理后台」由侧边栏 logo 块移入顶部 Header（28px 蓝底圆角 logo-mark + 18px 标题），与用户端「星楼KYC」位置及样式完全一致。
  - 侧边栏对齐：移除管理端侧边栏多余的 box-shadow 与 60px 的 logo 块（菜单起始位置与用户端一致），统一为 200px 白底+右边框。
  - 菜单项对齐：菜单项高度统一为 44px（用户端由 42px 调整为 44px），间距统一为 margin 0 8px、图标间距 12px，圆角统一为 6px，激活/悬停态样式两端一致。

## v1.4.3
- 全站品牌形象与布局层级统一：
  - 新增全局品牌标样式（.brand-logo 及 .brand-logo.lg 大号变体）：蓝色圆角方块（#006EFF）+ 白色 SL 字样，统一维护于 main.css。
  - 用户端后台 Header「星楼」星形图标、管理端后台 Header「管」字标识、首页顶部与页脚、文档中心「K」字标识全部替换为统一的 SL 品牌标。
  - 登录/注册页左侧品牌区星形 SVG 替换为大号 SL 品牌标，保留「StarLoft / KYC Authentication」品牌文字。
  - 浏览器标签页图标更换为与品牌一致的 SL 版 favicon.svg（index.html 引用由失效的 /vite.svg 修正为 /favicon.svg），并删除遗留的默认 favicon.ico。
  - 布局层级对齐：管理端由「左侧导航栏为一级、Header 位于侧边栏右侧」重构为与用户端一致的「Header 全宽置顶为一级、左侧导航栏位于 Header 下方为二级」，消除两端层级差异。

## v1.4.4
- 统一用户端与管理端侧边栏 LOGO 与菜单项的精确位置：
  - LOGO 位置对齐：管理端折叠按钮由品牌标左侧移至其右侧，品牌标（SL + 标题文字）与用户端一样从 Header 最左侧开始，消除两端 LOGO 起始位置差异。
  - 菜单项几何对齐：管理端 el-menu-item 水平内边距由 Element Plus 默认 20px 调整为 16px（与用户端一致），图标由默认 18px 统一为 20px、图标间距统一为 12px，字号/字重与用户端一致（14px/500），使两端每个菜单项的图标与文字水平位置、垂直居中完全一致。

## v1.4.5
- 管理端侧边栏重构为与用户端完全一致的结构，彻底消除 LOGO 与菜单项的位置差异：
  - LOGO 位置修复：管理端 Header 品牌标此前因折叠按钮前置被向右挤出，与用户端「星楼KYC」不在同一位置；现移除折叠按钮，品牌标与用户端一样从 Header 最左侧开始（距左缘 24px）。
  - 侧边栏结构统一：管理端由 Element Plus el-menu 替换为与用户端相同的自定义侧边栏（layout-sidebar/sidebar-nav/sidebar-item + 内联 SVG 图标），菜单项高度（44px）、宽度（184px）、图标尺寸（20px）、图标间距（12px）、圆角（6px）、激活/悬停态样式与用户端逐像素一致，文字与图标水平位置完全相同。
  - 移除管理端侧边栏折叠功能（isCollapse/toggleCollapse 及无用的 el-menu 依赖），Header 右侧保留「修改密码/退出登录」下拉菜单。

## v1.5.0
- 新增用户名与邮箱字段，注册全面改为三要素（用户名+手机号+邮箱）必填：
  - 用户名仅支持英文、数字、下划线，长度 3-32 位，全平台唯一；邮箱、手机号同样不可重复；注册接口返回对应错误提示（「该用户名已被占用」「该邮箱已被注册」等）。
  - 注册需同时通过手机验证码与邮箱验证码双验证；新增邮箱验证码发送接口（POST /api/v1/user/send-email-code），基于 SMTP 发送并存储至 Redis（5 分钟有效，含 60 秒发送频率限制）。
  - 登录支持用户名、手机号、邮箱三种账号（密码登录），验证码登录仍仅支持手机号。
  - 管理后台手动注册用户增加用户名、邮箱必填项；用户列表、用户详情同步展示用户名与邮箱。
- 新增日志文件化落盘（/app/logs，docker-compose 已映射 backend_logs 数据卷），按种类分文件记录、按大小自动滚动，重启/删除容器日志不丢失：
  - access.log 记录全部 HTTP 请求；business.log 记录实名认证等业务日志；error.log 记录错误与异常；admin.log 记录管理后台操作日志。
- 新增管理后台操作日志：登录、手动注册、禁用/启用用户、删除用户、人工充值、修改配置、资源包管理等操作统一写入 admin.log 文件（不额外创建数据表，遵循「日志仅落文件」原则）。

## v1.5.1
- 撤销 v1.5.0 中误引入的 `admin_operation_log` 数据表，管理后台操作日志回归纯文件记录：
  - 移除 admin_operation_log 建表语句与数据模型、仓储、查询接口（GET /api/v1/admin/logs）及管理后台「操作日志」页面与菜单入口。
  - 管理后台各类操作（登录/注册/禁用启用/删除/充值/改配置/资源包管理等）继续写入 /app/logs/admin.log，无任何功能损失。
- 移除 init.sql 中「存量数据库升级脚本」一节（含存量用户补全 username/email 的 ALTER 语句与建表注释），保持建表脚本仅面向全新安装。

## v1.5.2
- 修复前端构建报错（vue-tsc 编译失败，导致 Docker 前端镜像构建中止）：
  - 登录接口类型定义中 `account` 字段由必填调整为可选，与短信验证码登录（仅传手机号）的实际调用一致，消除 TS2345 类型不匹配错误。

## v1.5.3
- 修复用户端无法查看实名认证单价的问题，并修复本次改动引入的构建失败：
  - 公开配置接口（GET /api/v1/public/config）新增返回 `kyc_price`（平台 KYC 认证单价，来源于系统配置），无需登录即可获取。
  - 首页「价格方案」由写死的 0.50 元改为动态读取平台实名认证单价，与后台配置实时一致。
  - 用户中心首页（Dashboard）新增「实名认证单价」卡片，展示当前实名单价（元/次）。
  - 修复 Dashboard.vue 脚本区遗漏 kycPrice 相关声明（publicAPI 导入、kycPrice 状态、loadKycPrice 加载函数与 onMounted 调用），导致模板引用 kycPrice 报 TS2339 构建失败的缺陷，补齐后恢复。

## v1.5.4
- 修复前端报错弹窗重复弹出的问题（一次操作可能唤起 2~3 个重复报错弹窗）：
  - 根因：请求拦截器已统一弹出业务错误，页面 catch 块又各自再弹一次相同的错误消息，造成重复弹窗。
  - 处理：在 main.ts 全局覆写 `ElMessage.error`，对相同错误消息在 1.5 秒内重复弹出时自动去重，仅保留一个；不影响不同错误消息的正常提示。
- 修复全站接口返回 502（Bad Gateway）导致页面无法加载数据的问题（后端服务不可用）：
  - 根因：后端容器以非 root 用户（app，uid 1000）运行，但后端 Dockerfile 未在镜像中创建 `/app/logs` 目录。Docker 挂载命名卷时，对镜像中不存在的目录会以 root 属主创建，导致容器内 app 用户无法写入日志文件（`open /app/logs/business.log: permission denied`），日志初始化失败，后端启动即崩溃，Nginx 反向代理无法连接后端，所有接口返回 502。
  - 处理：后端 Dockerfile 增加 `RUN mkdir -p /app/logs && chown -R app:app /app`，确保新建卷具备 app 用户写权限；部署时需删除服务器上已存在的旧 backend_logs 卷（root 属主）后重建后端容器。

## v1.5.5
- 用户界面标注账户实名剩余免费认证次数：
  - 背景：账户实名（Web 路径）默认免费，但账号终身累计失败（status=3）达到 3 次后转为计费（先扣资源包、再扣余额）；此前用户无法在界面看到剩余免费次数。
  - 后端：AuthService 新增 `GetFreeAuthRemaining`（复用现有免费失败上限常量与统计方法）；用户资料接口（GET /user/profile）新增返回 `free_auth_limit`（免费上限，3 次）与 `free_auth_remaining`（剩余免费次数）。
  - 前端：
    - 用户中心（Dashboard）「个人信息」卡片新增「剩余免费认证」行，未实名显示剩余次数（如 3/3 次），已实名显示「已实名（无需再认证）」。
    - 实名认证页（/user/kyc）认证表单上方新增醒目提示：剩余免费次数用绿色标签展示；次数用完后以黄色标签提示「后续认证将按平台单价收费」。

## v1.5.6
- 邮箱验证码发送由 SMTP 切换为腾讯云 SES（Simple Email Service）官方 API（SendEmail）：
  - 背景：原邮箱验证码依赖 SMTP 发送，需额外维护 SMTP 账号与密码；现统一使用腾讯云 API 鉴权（复用 TENCENT_SECRET_ID / TENCENT_SECRET_KEY），与短信服务保持一致。
  - 后端：EmailService 重写为基于腾讯云 SES SDK（ses/v20201002）的模板发送，发送频率限制、验证码生成与校验逻辑保持不变；未配置 SES（发件人邮箱或模板ID为空）时返回 nil，不启用邮箱验证码功能。
  - 配置：邮件配置由 SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASSWORD/SMTP_FROM 调整为 SES_FROM（发件人邮箱）、SES_TEMPLATE_ID（发信模板ID）、SES_REGION（地域，默认 ap-guangzhou）；go.mod 新增 `tencentcloud/ses v1.3.155` 依赖。
  - 部署：需在腾讯云邮件推送控制台完成发信域名验证、创建发信地址与发信模板（模板正文需包含验证码变量 `{{code}}`），并在 .env 中填写 SES_FROM / SES_TEMPLATE_ID；部署前需执行 `go mod tidy` 拉取新增依赖。

## v1.5.7
- 修正 go.mod 中 `tencentcloud/ses` 依赖版本号：v1.3.155 修正为 v1.3.136。
  - 原因：腾讯云 Go SDK 为单仓库多模块，各子模块（common/sms/ses）独立发版、版本号不同步；ses 模块不存在 v1.3.155 版本（v1.3.x 系列最高为 v1.3.136），导致 `go mod download` 报 `unknown revision tencentcloud/ses/v1.3.155` 构建失败。
  - 处理：将 ses 依赖版本修正为实际存在的 v1.3.136；重新构建前需先执行 `go mod tidy` 更新 go.sum。

## v1.5.8
- 修复后端 Docker 构建失败：`go build` 报 `missing go.sum entry`（腾讯云 ses 模块）。
  - 原因：宿主机 go.sum 缺少 ses 模块校验和条目；原 Dockerfile 先 `COPY go.mod go.sum` 并 `go mod download`（容器内已写入 ses 条目），随后 `COPY . .` 会用宿主机旧 go.sum 覆盖容器内更新后的 go.sum，导致编译时校验和缺失。
  - 处理：调整 backend/Dockerfile，将 `go mod download` 移至 `COPY . .` 之后执行，保证容器内生成的 go.sum 校验和完整保留后再编译，构建无需在宿主机手动执行 `go mod tidy`。

## v1.5.9
- 修复后端 Docker 构建仍报 `missing go.sum entry`：将 backend/Dockerfile 的 `go mod download` 调整为 `go mod tidy`。
  - 原因：`go mod download` 仅按 go.mod 中的 require 下载模块，实测未将 ses 模块校验和写入 go.sum，`go build` 编译时仍提示缺少 ses 的 go.sum 条目。
  - 处理：在 `COPY . .` 之后改用 `go mod tidy`，扫描源码 import 重建 go.mod/go.sum 并补齐所有缺失校验和，确保编译前依赖与校验和完整。

## v1.5.10
- 平台流水号 platform_biz_no 生成格式调整：由 `{biz_no}_{user_id}_{纳秒时间戳}` 三段式改为纯随机数。
  - 后端：AuthService 生成平台流水号时不再拼接业务前缀，改为 `utils.GenerateRandomDigits(20)` 生成 20 位随机数字（密码学安全随机源 crypto/rand），保证唯一性且不暴露业务信息；utils 新增 `GenerateRandomDigits` 函数。
  - 文档：同步更新 zjmf_v10/zjmf_mfcw 插件 README、zjmf_mfcw INTEGRATION 及前端 API 文档中的 platform_biz_no 示例值为纯随机数字格式。

## v1.6.0
- 新增内部账号功能：单独建表记录本司其他系统专用账号，无需实名认证与计费，无法在用户端登录，创建后可在管理后台直接查看/重置 API：
  - 数据库：init.sql 新增 `internal_account` 表（账号名称 name 唯一、api_key 唯一、api_secret、status、remark、创建/更新时间）。
  - 后端：新增内部账号模型与仓储（创建/列表/启禁用/重置密钥）；API Key 鉴权中间件优先匹配内部账号（命中后写入 is_internal 标记，user_id 为内部账号 ID，无需实名、不计费），未命中再匹配平台用户；发起认证服务支持内部账号分支（跳过平台用户查询、强制不计费、不扣余额与资源包）。
  - 认证结果同步与异步回调对内部账号跳过平台用户实名信息绑定与 API Secret 下发（内部账号实名信息不写入 platform_user），下游异步通知改用内部账号的 API Secret 生成签名。
  - 管理后台新增「内部账号」页面（/admin/internal-accounts）：支持创建账号（自动生成 32 位 API Key/Secret 并提示复制保存）、查看并复制密钥、重置 API（Key/Secret 一并重新生成，旧密钥立即失效）、启用/禁用。
  - 内部账号调用平台 KYC 接口（/api/v1/kyc/start、/kyc/result、/kyc/balance/query）时跳过实名校验，查询余额接口返回 is_internal=true 且不计费。

## v1.7.0
- 支付渠道切换：取消银联天满支付，改为接入支付宝与微信支付官方接口：
  - 支付宝：电脑网站支付（alipay.trade.page.pay），RSA2 签名/验签；后端新增 AlipayClient，充值按渠道返回跳转链接。
  - 微信支付：APIv3 Native 扫码支付，商户私钥签名、微信支付公钥验签 + AES-GCM 回调解密；后端新增 WeChatPayClient，充值返回支付二维码。
  - 新增公共二维码渲染接口（GET /api/v1/public/qr）；新增支付宝/微信异步回调处理器（/api/v1/callback/alipay、/api/v1/callback/wechat），删除银联回调处理器与银联支付客户端。
  - 前端充值页支付方式改为「支付宝 / 微信支付」：支付宝新窗口打开支付链接，微信弹窗展示二维码并轮询订单状态（GET /user/recharge/result）直至到账；管理后台订单页筛选与映射同步更新。
  - 配置切换：.env/.env.example/docker-compose 中银联配置（UNIONPAY_*）替换为支付宝/微信配置（ALIPAY_* / WECHAT_*），密钥支持单行书写（换行用字面 \n 表示）。
  - 移除全站所有银联相关内容（支付选项、后台筛选、首页文案、配置与文档），系统中不再留存任何银联信息；实名认证上游为原力金智（FinAuth）。
- 回调/return 地址写死进代码：
  - 支付：支付宝异步通知与同步 return 地址（alipay.go）、微信异步通知地址（wechatpay.go）改为写死，移除 ALIPAY_NOTIFY_URL / ALIPAY_RETURN_URL / WECHAT_NOTIFY_URL 环境变量。
  - 实名认证：上游（FinAuth）异步通知地址写死（auth_service.go 的 finAuthNotifyURL，即 /api/v1/callback/finauth）；return_url 透传下游/用户地址（认证完成后浏览器直接返回下游），原 FINAUTH_RETURN_URL 配置参数已无用途而移除；未传 return_url 时兜底返回账户实名页（/user/kyc）。
  - 配置文件中相应移除上述环境变量，并注明地址已写死。
- 清理死代码与测试性质代码：
  - 删除未被任何代码使用的 kyc_api_record 数据表及对应仓储；清理 KycRecordRepository 的 GetByID/GetByUserID/GetAllRecords 与 SystemConfigRepository.UpdateConfig 等死方法；移除 revertStartAuth 未使用参数。
  - 移除调试性打印验证码等测试功能，规范代码注释（仅解释代码客观内容，移除教程/示例类表述）。

## v1.8.0
- 整合自 v1.6.0 以来的全部更新，作为正式汇总版本：
- 支付渠道切换：取消银联天满支付，改为接入支付宝与微信支付官方接口：
  - 支付宝：电脑网站支付（alipay.trade.page.pay），RSA2 签名/验签；充值按渠道返回跳转链接。
  - 微信支付：APIv3 Native 扫码支付，商户私钥签名、微信支付公钥验签 + AES-GCM 回调解密；充值返回支付二维码。
  - 新增支付异步回调处理器（/api/v1/callback/alipay、/api/v1/callback/wechat）与公共二维码渲染接口（GET /api/v1/public/qr），删除银联支付客户端与回调。
  - 前端充值页支付方式改为「支付宝 / 微信支付」：支付宝新窗口打开支付链接，微信弹窗展示二维码并轮询订单状态（GET /user/recharge/result）；管理后台订单筛选与映射同步更新。
  - 配置切换：银联配置（UNIONPAY_*）替换为支付宝/微信配置（ALIPAY_* / WECHAT_*），密钥支持单行书写（换行用字面 \n 表示）。
  - 移除全站所有银联相关内容（支付选项、后台筛选、首页文案、配置与文档），实名认证上游为原力金智（FinAuth）。
- 回调/return 地址写死进代码，移除对应环境变量配置：
  - 支付：支付宝异步通知与同步 return 地址（upstream/alipay.go）、微信异步通知地址（upstream/wechatpay.go）写死；移除 ALIPAY_NOTIFY_URL / ALIPAY_RETURN_URL / WECHAT_NOTIFY_URL。
  - 实名认证：上游异步通知地址写死（auth_service.go 的 finAuthNotifyURL，即 /api/v1/callback/finauth）；return_url 透传下游/用户地址（认证完成后浏览器直接返回下游），移除原 FINAUTH_RETURN_URL 配置；未传 return_url 时兜底返回账户实名页（/user/kyc）。
  - 配置文件中相应移除上述环境变量，并注明地址已写死。
- 清理死代码与测试性质代码，规范代码注释：
  - 删除未被任何代码使用的 kyc_api_record 数据表及对应仓储；清理 KycRecordRepository 的 GetByID/GetByUserID/GetAllRecords 与 SystemConfigRepository.UpdateConfig 等死方法；移除 revertStartAuth 未使用参数。
  - 移除调试性打印验证码等测试功能；注释仅解释代码客观内容，移除教程/示例类表述。

## v1.8.1
- 清理 .env / .env.example / docker-compose.yml 中针对已删除字段（回调/return 地址等）的解释性注释，仅保留对现存字段含义的注释。

## v1.8.2
- 管理后台用户管理页的「手动注册用户」按钮文案改为「人工注册」。

## v1.8.3
- 数据库建表由 init.sql 迁移至后端 GORM AutoMigrate：
  - 后端启动时按模型自动创建/更新表结构（复用现有数据库连接，表名保持单数）；各模型补充 gorm 标签（列名、类型、长度、唯一/普通索引、默认值），新增 system_config 模型用于建表。
  - 删除未被任何代码使用的 callback_log 建表语句（无对应模型与代码引用）。
  - init.sql 移除全部建库/建表 DDL，仅保留初始化数据（默认管理员、系统配置 kyc_price、12 个资源包套餐）。
  - 部署顺序调整为：先启动后端完成建表，再执行 init.sql 插入初始化数据；README 部署说明同步更新。

## v1.9.0
- 接入 acme.sh 实现 HTTPS 证书自动续期（替换腾讯云免费证书手动续期的方案）：
  - 新增 acme-sh 容器（neilpang/acme.sh）：基于腾讯云 DNSPod DNS API 自动完成域名验证，复用 TENCENT_SECRET_ID/TENCENT_SECRET_KEY（需具备 DNSPod 解析权限），证书写入宿主机 ./certs（fullchain.pem / privkey.pem），自动续期后由 Nginx 自动加载。
  - 前端 Nginx 容器新增入口脚本：后台启动 Nginx，每 5 分钟检测证书文件指纹，变化即 reload 加载新证书；Nginx 异常退出自动拉起（避免引入 docker.sock 挂载的安全风险）。
  - 配置新增 ACME_EMAIL（ACME 账号邮箱，接收续期提醒）；docker-compose 新增 acme_data 数据卷。
  - README 部署步骤更新：不再手动放置证书，改为先启动 acme-sh 注册账号并申请安装证书（--issue --dns dns_tencent --server letsencrypt），再启动全部服务。

## v1.9.1
- 回退证书自动续期功能：腾讯云支持证书自动续费，无需 acme.sh。
  - 移除 docker-compose 中的 acme-sh 容器与 acme_data 数据卷，删除 .env / .env.example 中 ACME_EMAIL 配置。
  - 前端 Nginx 移除证书监听入口脚本，恢复直接启动 nginx；证书仍从宿主机 ./certs 目录挂载引用（fullchain.pem / privkey.pem），证书由服务商（腾讯云）自动续费，到期后替换文件并重启前端加载。
  - README 部署步骤同步更新为手动放置证书。

## v1.9.2
- 修复启动日志刷屏：AutoMigrate 内省查询在云数据库上耗时较长，GORM 慢查询日志（阈值 200ms）在启动时大量输出；将 AutoMigrate 的 GORM 日志级别调整为 Error，仅记录真实错误。

## v1.9.3
- 修复 v1.9.2 引入的编译错误：db.go 使用了 logger 但遗漏 `gorm.io/gorm/logger` 导入，补齐该 import（此前导致后端镜像构建失败 undefined: logger）。

## v1.9.4
- 修复未配置支付时启动崩溃：支付宝/微信支付客户端在启动时即解析 RSA 密钥，.env 中未填写的占位符（your_ 开头）会被当作真实值解析导致 panic；构造函数现把占位符视为未配置并跳过创建客户端，系统可在未配置支付的情况下正常启动。

## v1.9.5
- 回退 v1.9.4 的占位符判断逻辑：保留原始的"空字符串即未配置"判断，支付配置未使用时需将 .env 中支付宝/微信各项置空即可正常启动。

## v1.9.6
- 移除支付宝网关配置 ALIPAY_GATEWAY：生产环境固定使用官方网关，无需可配置项。删除 config 的 Gateway 字段与环境变量加载、AlipayClient 的 Gateway 字段与构造参数（改用内置默认网关常量）、docker-compose/.env/.env.example 中的 ALIPAY_GATEWAY；同时清理 v1.9.5 遗留的死代码 isConfiguredValue。

## v1.10.0
- 平台化改造：单一实名认证网站升级为多产品云平台（对标腾讯云/阿里云），门户 + 独立产品 + 统一控制台：
  - 新增门户应用 frontend-portal（部署于 www.starloft.cn）：平台首页、产品页（实名认证 /kyc、云服务器 /cs、短信服务 /sms）、文档中心（/docs）；产品页按路由 meta 复用 ProductPage.vue，新增产品只需在 src/config/products.ts 登记并注册路由。
  - 控制台应用（console.starloft.cn）路由重构为产品化结构：去除 /user 前缀，实名认证 /kyc、云服务器 /cs、短信服务 /sms 等均为顶层产品路径；侧边栏按「产品服务 / 资源与账户」分组；未上线产品新增 ComingSoon 占位页。
  - 文档中心从控制台迁移至门户：API 文档、智简魔方插件教程迁至 www.starloft.cn/docs；控制台「API 管理」页文档入口改为跳转门户文档。
  - 部署架构调整：前端镜像一次构建门户与控制台两个应用，Nginx 按域名分发（www.starloft.cn → 门户，console.starloft.cn → 控制台），旧域名 kyc.starloft.cn 端口 80 重定向至门户；证书目录调整为 certs/www.starloft.cn 与 certs/console.starloft.cn 两个子目录分别存放。
  - 平台 API 域名统一为 www.starloft.cn：后端硬编码回调地址（支付宝/微信异步通知、上游实名认证异步通知）由 kyc.starloft.cn 改为 www.starloft.cn；控制台支付宝同步 return 地址指向 console.starloft.cn/balance；插件默认 API 地址与各文档示例同步更新。

## v1.11.0
- 数据库分库改造：单库拆分为「系统库 + 产品库」多库架构（同一 MySQL 实例下的多个 database，事务可跨库）：
  - 新增系统库 starloft_sys：平台用户、管理员、内部账号、实名记录、余额流水、充值订单、系统配置、登录日志；新增实名认证产品库 starloft_kyc：认证订单、资源包；预留云服务器产品库 starloft_cs、短信服务产品库 starloft_sms。
  - 新增用户/管理员登录日志表（user_login_log / admin_login_log），用户登录与管理端登录成功/失败均落库记录账号、IP、UA、原因。
  - 配置新增 KYC_DB_NAME（实名认证产品库名），后端连接默认库为系统库，产品库表通过全限定库名跨库访问。
  - 所有模型 TableName 与仓储/统计原生 SQL 统一使用全限定表名（starloft_sys.xxx / starloft_kyc.xxx），保证分库后正确路由。
  - 重建 init.sql：创建 4 个数据库与全部表结构，初始化默认管理员、系统配置与资源包种子数据；旧库无存量数据可直接删除重建。

## v1.11.1
- init.sql 增加删除重建能力：脚本头部 DROP 语句由注释态改为可执行，执行脚本会先删除旧库再重建全部库表（避免旧表/旧字段残留，AutoMigrate 只增不减无法清理），README 使用说明同步更新。

## v1.11.2
- 修复 init.sql 初始化数据执行失败：admin_user/system_config/resource_pack 的 created_at、updated_at 为 NOT NULL 且无默认值，直接插入报 ERROR 1364；3 处初始化数据补上 NOW(3) 时间字段，脚本可完整执行。

## v1.12.0
- 取消内部账号功能：移除 internal_account 表与内部账号管理（后端模型/仓储/4 个管理接口、API Key 鉴权中间件内部账号分支、认证服务内部账号分支与不计费逻辑、管理后台「内部账号」页面与路由/菜单/API）。
- API Key 鉴权中间件统一按平台用户（platform_user）校验；认证订单均需归属平台用户，实名成功统一绑定用户实名信息并下发 API Secret，下游通知统一使用平台用户 API Secret 生成签名。
- init.sql 移除 internal_account 建表语句。

## v1.12.1
- 控制台前端目录重命名 `frontend` → `frontend-console`，明确其对应 console.starloft.cn 控制台；同步更新 Dockerfile COPY 路径、docker-compose 服务名（`frontend` → `frontend-console`，容器名 `kyc_frontend` → `kyc_frontend_console`）、.dockerignore/.gitignore、README。
- 部署命令相应调整为 `docker compose up -d --build frontend-console`。

## v1.13.0
- 管理后台剥离为独立前端应用 frontend-admin（部署于 admin.starloft.cn）：
  - 新建 frontend-admin 应用：独立 package/vite/tsconfig/main/router/store/api/request，管理页面（数据统计 / 用户管理 / 认证订单 / 资源包 / 系统配置）与 AdminLayout 迁移至此，路由调整为根路径（/dashboard、/users、/orders、/packs、/config），管理员状态使用独立 useAdminStore（admin_token），登录页「非管理员请前往」链接指向用户控制台。
  - 控制台应用 frontend-console 移除管理端：删除 views/admin 与 AdminLayout，裁剪管理相关路由、API、useAdminStore 状态，清理 main.css 中仅管理端使用的 stats-grid 死代码。
  - 部署架构调整：前端镜像一次构建门户、控制台与管理后台三个应用，Nginx 新增 admin.starloft.cn 站点块（证书目录 certs/admin.starloft.cn，/api/ 反向代理至 backend）；docker-compose 证书挂载目录、README 架构/部署/项目结构同步更新。
- 部署：放置 admin.starloft.cn 证书后执行 `docker compose up -d --build frontend-console` 重建前端镜像。
