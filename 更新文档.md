v1.0.0
首个版本

v1.0.1
修复了认证接口返回信息类型错误的问题

v1.0.2
下游调用改为平台中转：认证结果先回到平台，再由平台通知/跳转下游；/kyc 页面展示结果并倒计时返回，新增“我已完成验证，点击此处”手动查询上游

v1.0.3
/kyc 页面统一使用 biz_no 参数；认证中状态下即显示“我已完成验证，点击此处”手动查询按钮，并保留继续认证入口
修复账户实名计费：订单金额按用户实际 KYC 单价记录，实名界面实时显示用户 KYC 单价（此前均固定为 1 元）

v1.0.4
后台认证订单列表补充显示用户手机号（订单查询联表获取用户）；订单失败状态展示失败原因（result_message 或 result_code）

v1.0.5
修复认证结果同步：上游返回「未开始（NOT_STARTED）/进行中（PROCESSING）」时保持订单「认证中」，不再误判为失败并结束流程

v1.0.6
前端页面增加加载动画：账户实名、首页、API 管理、余额、个人资料等页面在数据加载完成前显示加载态，避免先显示默认值再闪变

v1.0.7
修复姓名校验：正则改用 Go 支持的 Unicode 类 \p{Han}（原 \uXXXX 写法在 Go 中恒报错，导致所有姓名被判为非法），长度改为按字符数而非字节数判断，并容忍首尾空格

v1.0.8
新增管理员修改密码：后台右上角登录账号下拉菜单新增“修改密码”，需校验旧密码并二次确认新密码
统一时间显示格式：所有页面时间均去除末尾时区信息（+08:00），仅展示 YYYY-MM-DD HH:mm:ss
