v1.0.0
首个版本

v1.0.1
修复了认证接口返回信息类型错误的问题

v1.0.2
下游调用改为平台中转：认证结果先回到平台，再由平台通知/跳转下游；/kyc 页面展示结果并倒计时返回，新增“我已完成验证，点击此处”手动查询上游

v1.0.3
/kyc 页面统一使用 biz_no 参数；认证中状态下即显示“我已完成验证，点击此处”手动查询按钮，并保留继续认证入口
修复账户实名计费：订单金额按用户实际 KYC 单价记录，实名界面实时显示用户 KYC 单价（此前均固定为 1 元）

v1.0.4
后台认证订单列表补充显示用户手机号（订单查询联表获取用户）；订单失败状态展示失败原因（result_message 或 result_code）

v1.0.5
修复认证结果同步：上游返回「未开始（NOT_STARTED）/进行中（PROCESSING）」时保持订单「认证中」，不再误判为失败并结束流程

v1.0.6
前端页面增加加载动画：账户实名、首页、API 管理、余额、个人资料等页面在数据加载完成前显示加载态，避免先显示默认值再闪变

v1.0.7
修复姓名校验：正则改用 Go 支持的 Unicode 类 \p{Han}（原 \uXXXX 写法在 Go 中恒报错，导致所有姓名被判为非法），长度改为按字符数而非字节数判断，并容忍首尾空格

v1.0.8
新增管理员修改密码：后台右上角登录账号下拉菜单新增“修改密码”，需校验旧密码并二次确认新密码
统一时间显示格式：所有页面时间均去除末尾时区信息（+08:00），仅展示 YYYY-MM-DD HH:mm:ss

v1.0.9
修复发起认证的异常兜底：扣费失败时回滚刚创建的订单与实名记录，避免产生「待认证」但无上游 token 的悬挂订单；写入上游 token 失败时退还预扣余额并将订单标记为超时退款
修复 /kyc 结果页跳转：认证完成/失败后优先返回下游 return_url；return_url 缺失时退回来源页，不再误跳平台登录页

v1.0.10
修复用户账号实名回跳地址：向上游传递的 return_url 由不存在的 /user/kyc/result 统一改为 /kyc/result；前端 /auth-result 结果页路由与后端同名占位路由、回调重定向一并更名为 /kyc/result

v1.0.11
新增文档中心 /docs：文档总目录（/docs）、API 文档目录（/docs/api）、API v1 详细调用文档（/docs/api/v1）、插件使用教程目录（/docs/plugin）及智简魔方（ZJMF）插件教程（/docs/plugin/zjmf）
去除用户中心「API 管理」页面的接口调用提示信息，改为跳转文档中心查看
修复后台用户列表实名信息显示为 undefinedundefined：GetUserList 未提取 sql.NullString 而直接序列化导致，改为提取 kyc_name/kyc_id_card 字符串值

v1.0.12
平台实名认证增加年龄限制：要求年满16周岁，发起认证（Web 账户实名与 API 调用）时根据身份证号中的出生日期计算周岁年龄，未满16周岁返回“实名认证需年满16周岁”并拒绝发起
首页「个人信息」栏在邮箱与注册时间之间增加「实名单价」展示

v1.0.13
补充 API 文档（/docs/api/v1）回调说明：新增异步通知（notify_url）与同步跳转（return_url）章节，说明通知请求体字段（biz_no/platform_biz_no/status/result_code/result_message/cost）、result_code 结果码含义（1000/2000/3000/4000/6000/6100，其中 6000/6100 不计费自动退款），以及 return_url 以 GET 302 跳转且不附加参数、需下游主动调用查询接口落地结果

v1.0.14
修复账户实名 return_url 页面 404：将 Web 端发起认证的 usePlatformURLs 由 false 改为 true，使上游认证完成后先经平台 /kyc 中转页（KYCVerify）按 biz_no 查询结果，再跳转回用户实名页；同时将 KYC.vue 的 return_url 由 /kyc/result 改为 /user/kyc，认证完成后正确跳回「账户实名」页面
清理遗留死代码：删除已废弃的前端 /kyc/result 结果页路由及其 AuthResult.vue、后端同名占位路由 GET /kyc/result，以及未注册路由的 FinAuthReturnCallback 同步回调处理函数
修正账户实名后端默认 return_url：将 StartAuthForWeb 的空缺回跳地址默认值由占位地址 https://yourdomain.com/kyc/result 改为 /user/kyc，与前端实际使用地址一致

v1.0.15
修复插件一次点击导致平台重复发起验证：
- 后端：StartAuth 增加幂等去重——同一 biz_no 已存在进行中订单（status 0/1）时直接复用并返回已有 platform_biz_no/auth_url，不再重复创建与扣费；GetOrderByBizNo 增加 ORDER BY id DESC LIMIT 1 确保取到最新订单
- 插件：ZjmfMfcwPlugin.php 将每次随机生成的 biz_no（日期+随机数）改为「同一用户+同一身份证」的固定值（ZJMF_uid_身份证MD5前8位），配合后端去重避免重复创建任务

v1.0.16
修复 admin token 过期后被路由守卫/401 拦截器错误跳转到 /login 而非 /admin/login，导致无法登录的问题：
- 前端 request.ts 响应拦截器：401 时按请求路径区分管理员接口（url 以 /admin/ 开头）与普通用户接口，管理员接口清除 admin_token 并跳转 /admin/login，普通接口跳 /login

v1.1.0
业务调整：账户实名永久绑定且免费，API 需实名后开通：
- 账户实名认证不可修改：实名成功后信息永久绑定，移除「更换实名」接口（后端 /user/kyc/replace 路由、服务与仓储层 Replace/ClearUserKYCInfo 方法、前端更换实名按钮与入口全部删除）
- 账户实名由收费改为免费：Web 端账户实名（/user/kyc）不再扣取余额，仅 API 下游业务调用（/api/v1/kyc/start）仍按 KYC 单价从余额扣费；实名页移除余额校验与费用展示
- API 需实名后开通：注册不再生成 API Key，账户实名成功后自动下发 API Key/Secret；重置 API 密钥同样需先完成实名认证，否则返回 403
